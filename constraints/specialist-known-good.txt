# Dependency set for the out-of-process SPECIALIST venv (advanced Gemma planner).
#
# This is a SEPARATE venv from the main/OpenVLA one. A modern transformers here
# lets the planner run Gemma 2/3, free of OpenVLA's pinned transformers==4.40.1
# (which stays in the main venv via constraints/openvla-known-good.txt).
#
# Set up the specialist venv:
#   python -m venv ~/specialist-venv
#   ~/specialist-venv/bin/pip install -e ".[specialist]" -c constraints/specialist-known-good.txt
#   export ODYSSEY_SPECIALIST_PYTHON=~/specialist-venv/bin/python
#
# Gemma 3 needs transformers>=4.50. These are a coherent recent set; treat as a
# starting point and re-pin exact versions (incl. torch) once validated on the
# GPU box. The eval-side main venv is unaffected.
transformers==4.51.3
tokenizers==0.21.4
accelerate==1.13.0
bitsandbytes==0.49.2
sentencepiece==0.2.0
